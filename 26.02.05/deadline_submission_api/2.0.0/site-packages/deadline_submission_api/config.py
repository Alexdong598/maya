"""This module contains functionality to get config information from the global
config data.

"""
# Import third-party modules
from hal_config.config import Configuration  # pylint:disable=import-error


def get_config(key):
    """Read config file and return value by key.

    Args:
        key (str): Key to get value from the config file.

    Returns:
        str: Value from key.

    """
    config = Configuration()
    return config.query('deadline_api', key)
