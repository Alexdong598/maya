# Mocked configuration for `deadline_api`.
webservice:
  # Need to use ${HAL_BRANCH} syntax instead of just $HAL_BRANCH because
  # otherwise the environment variable isn't being resolved.
  hostname: test_hostname
  port: 123456

# Deadline repository path.
repository:
  location: $_TEMP_REPO_PATH
